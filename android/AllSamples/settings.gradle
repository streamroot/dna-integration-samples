// Sample apps
def samples = [
        'ExoPlayer',
        'PlayKit',
        'PlayKitOVPStarter',
        'VideoView',
        'Brightcove'
]

samples.each {
    def entry = ':' + it
    include entry
    project(entry).projectDir = new File('../' + it + '/app')
}